<Window x:Class="Metra.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Metra v3.0 - Login"
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundColor}">

    <Grid Background="#D8D6FE">
        <!-- Loading Overlay -->
        <Border Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                Panel.ZIndex="999">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Width="60" Height="60"
                             IsIndeterminate="True"
                             Foreground="{StaticResource HeaderColor}"/>
                <TextBlock Text="Yuklanmoqda..."
                           Foreground="White"
                           FontSize="16"
                           Margin="0,20,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Viewbox>
            <Border Background="#ffffff" Margin="50">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Background="{StaticResource HeaderColor}">
                        <StackPanel Orientation="Horizontal" Margin="20">
                            <materialDesign:PackIcon Kind="TruckFast"
                                                     Foreground="White"
                                                     Width="47" Height="47"
                                                     VerticalAlignment="Center"
                                                     HorizontalAlignment="Center"
                                                     Margin="0,0,15,0"/>
                            <TextBlock Text="Metra"
                                       FontSize="32"
                                       VerticalAlignment="Center"
                                       Foreground="White"
                                       FontWeight="Bold"/>
                            <TextBlock Text="v3.0"
                                       FontSize="14"
                                       VerticalAlignment="Bottom"
                                       Foreground="White"
                                       Margin="8,0,0,8"
                                       Opacity="0.8"/>
                        </StackPanel>
                    </Grid>

                    <!-- Login Form -->
                    <StackPanel Grid.Row="1"
                                Height="400" Width="400"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="20">

                        <TextBlock Text="Tizimga kirish"
                                   FontSize="28"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource HeaderColor}"
                                   Margin="0,0,0,40"/>

                        <!-- Phone Number -->
                        <TextBlock Text="Telefon raqam"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="1,0,0,8"/>

                        <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                                 Height="40"
                                 FontSize="16"
                                 materialDesign:HintAssist.Hint="+998 (__) ___-__-__"
                                 materialDesign:HintAssist.IsFloating="False"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 VerticalContentAlignment="Center"
                                 Padding="12,0,0,0"
                                 Margin="0,0,0,20">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>

                        <!-- Password -->
                        <TextBlock Text="Parol"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="1,0,0,8"/>

                        <PasswordBox x:Name="PasswordBox"
                                     Height="40"
                                     FontSize="16"
                                     materialDesign:HintAssist.Hint="*******"
                                     materialDesign:HintAssist.IsFloating="False"
                                     Style="{StaticResource MaterialDesignOutlinedRevealPasswordBox}"
                                     VerticalContentAlignment="Center"
                                     Padding="12,0,0,0"
                                     Margin="0,0,0,40"
                                     PasswordChanged="PasswordBox_PasswordChanged">
                            <PasswordBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                            </PasswordBox.InputBindings>
                        </PasswordBox>

                        <!-- Login Button -->
                        <Button Command="{Binding LoginCommand}"
                                Height="45"
                                Margin="0,0,0,0"
                                Style="{StaticResource PrimaryButtonStyle}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="Login"
                                                         Width="20" Height="20"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,10,0"/>
                                <TextBlock Text="Kirish"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <!-- Footer -->
                        <TextBlock Text="© 2025 Metra. Barcha huquqlar himoyalangan."
                                   FontSize="11"
                                   Foreground="Gray"
                                   HorizontalAlignment="Center"
                                   Margin="0,30,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Viewbox>
    </Grid>
</Window>
